# Database connection settings
# Update with your MySQL credentials
# Also update query or table names if needed

database:
  host: localhost
  port: 3306
  name: minecraft
  user: root
  password: ''

leaderboard:
  world: world
  x: 0
  y: 100
  z: 0
  interval: 200 # ticks (10 seconds)
  size: 5
